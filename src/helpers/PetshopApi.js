const PetApi = {
  login: async (email, password) => {
    // fazer a consulta ao Web Service/ Banco de dados

    return { error: "Funcionalidade Incompleta" };
  },
};

export default () => PetApi;
